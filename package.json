{
  "name": "playable-ads",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained HTML5 playable ads, one file per build.",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "dev": "node build.js && node serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
